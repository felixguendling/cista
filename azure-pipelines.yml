jobs:
  - job: msvc
    pool:
      vmImage: 'windows-latest'
    steps:
      - script: |
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          cmake --build cista-test .
          cista-test