jobs:
  - job: msvc
    pool:
      vmImage: 'windows-latest'
    steps:
      - script: |
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release -GNinja ..
          ninja cista-test
          cista-test